## Proof & Trust Enhancements

### Portfolio / Case Study Page (`projects.html`)
- Hero: “Recent Metro East Builds” with quick stats (city, scope, budget range).
- Gallery: 6 cards with `img`, `alt`, location, and short bullet (materials, timeline). Add `data-location` attribute for future filtering.
- Add “Project of the Month” highlight + CTA to `contact.html`.
- Schema: add JSON-LD `Project` entries for each featured build.

### Testimonials / Reviews Hub
- Expand `reviews.html` with embedded video testimonials (or short text) plus star rating timeline.
- Add “Review Highlights” block on homepage/service pages using the same quotes, and output Review schema for each new quote.
- Keep linking to Google/Angi/HomeAdvisor + your `reviews.html` form.

### Meet the Team (`team.html`)
- Short bios for Bill Sessions + foremen, emphasizing owner presence.
- Include trust badges (licensed, insured, BBB, Angi/HA logos).
- Insert a CTA “Owner-led estimates → contact.html”.
- Schema: `Person` entries referencing each team member (name, role, description, image).

### Structured Data / FAQ refresh
- Ensure each new page (city/service/comparison) includes FAQ blocks in HTML + JSON-LD snippet covering 3 local questions.
- Add `Review` schema for directory pages referencing the All-Pro aggregate rating (4.7 stars).
- On homepage, include small `How We Work` timeline and embed `HowTo` schema for “Request estimate within 24 hours.”

### Media
- Replace placeholders with real photos named for SEO (e.g., `belleville-deck-01.jpg`). Keep dimensions/ALT text consistent.
- Add “Project video teaser” (YouTube or Loom) embed on `projects.html`/`reviews.html` if available.

Need me to scaffold one of these new proof pages (projects/testimonials/team) next? 
